// math

// round number
let number = 4.1; // 4
let round1 = Math.round(number);
console.log(round1);

let number1 = 4.5; // 5

let round = Math.round(number1);
console.log(round);

// nearest lower round number
let lowerRound = Math.floor(number1);
console.log(lowerRound);

// nearest upper round number
let upperRound = Math.ceil(number);
console.log(upperRound);

// square root of number
let sqRoot = Math.sqrt(144.144);
console.log(sqRoot);

// pow of number
let value = Math.pow(2, 10); // 2*2*2
console.log(value);

// random number
let randomNumber = Math.random(); // 0 -- 1
let randomNum = randomNumber * 10000;
randomNum = Math.round(randomNum);
console.log(randomNum);

// date
